import { Suspense} from 'react';
import { Canvas } from '@react-three/fiber';
import { OrbitControls, Preload, use } from '@react-three/drei';

const Earth = () => {
  return (
    <div>Earth</div>
  )
}

export default Earth